# wikiwho-authorship

A wrapper script to show a WikiWho authorship pie chart in a Codex dialog.

## Development

Build the single-file userscript with:

```bash
npm install
npm run build
```

The bundled distribution file is written to `dist/Gadget-WikiWho-Authorship.js`.
